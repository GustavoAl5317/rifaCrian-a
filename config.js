/* =========================================================
   CONFIGURAÇÕES DA RIFA
   ---------------------------------------------------------
   O responsável pode editar apenas este arquivo.
   ========================================================= */
window.CONFIG_RIFA = {
  // Total de números da rifa (o cartaz mostra de 1 a 100)
  totalNumeros: 100,

  // Valor de cada número, em reais
  valorNumero: 30,

  // Número de WhatsApp do responsável (com DDI + DDD, somente dígitos).
  // Exemplo Brasil: 55 (país) + 11 (DDD) + 999999999 (número) => "5511999999999"
  // >>> TROQUE pelo WhatsApp real do responsável pela rifa <<<
  whatsapp: "5511972363949",

  // Chave PIX exibida no site
  chavePix: "rafapedrozo.s@gmail.com",

  // Senha da "Área do responsável" (troque por uma senha só sua)
  senhaAdmin: "rifa2026",

  // Números JÁ COMPRADOS por padrão (aparecem bloqueados para todos).
  // Ex.: [7, 15, 42]. Deixe [] se ninguém comprou ainda.
  // Dica: também dá para marcar pelo painel do responsável dentro do site.
  numerosVendidos: []
};
